# Godot 4+ specific ignores
.godot/
*.translation

# Build output (regenerate with export_presets.cfg, don't commit binaries)
build/
*.exe

# Claude Code local session state (not project config)
.claude/scheduled_tasks.lock
